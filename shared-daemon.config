[core]
        fetchInterval = 60
[daemon]
        classpath = lib
        javaOptions = -client -noverify -Djava.awt.headless=true -Djna.nosys=true
        idleTimeout = infinity
        pidFile = shared-daemon.pid